package com.goldys.platform.raw;

/**
 * In-scope MVP sources only (spec Requirement 4). Tenzo is deliberately excluded - out of scope for
 * both MVP and the full build per the PRD's Non-Goals; do not add it here without updating the spec
 * first.
 */
public enum SourceSystem {
  LIGHTSPEED,
  COOKING_THE_BOOKS,
  OPENTABLE,
  DEPUTY,
  MANUAL_ENTRY
}
