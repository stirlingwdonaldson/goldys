.DS_Store
Claude outputs/

# Playwright MCP session output (page snapshots, console logs)
.playwright-mcp/

# Local Git worktrees used for isolated feature development
.worktrees/

# Impeccable ships a ~16MB platform binary that its launcher re-fetches from a
# version-pinned cache when absent; committing it would bloat every clone with a
# non-diffable, platform-specific artifact.
.opencode/skills/impeccable/scripts/bin/

# Local environment files (secrets). The .example files are the committed templates.
.env
.env.*
!.env.example
!.env.oidc.example
